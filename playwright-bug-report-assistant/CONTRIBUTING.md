# Contributing

Use Node.js 20+. Run `npm ci`, `npm run typecheck`, `npm test`, `npm run build`, and `npm run pack:check`. Use synthetic data only. Provider behavior must be mock-tested and CI must not require API access.
